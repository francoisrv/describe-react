# Be

## Child

```jsx
<To be a child of={ CHILD_OF } />
<To be only child of={ CHILD_OF } />
<To be single child of={ CHILD_OF } />
<To be first child of={ CHILD_OF } />
<To be last child of={ CHILD_OF } />
<To be child number={5} of={ CHILD_OF } />
<To be child at={5} of={ CHILD_OF } />
```

### Direct child

```jsx
<To be a direct child of={ CHILD_OF } />
<To be first direct child of={ CHILD_OF } />
<To be last direct child of={ CHILD_OF } />
<To be direct child number={5} of={ CHILD_OF } />
<To be direct child at={5} of={ CHILD_OF } />
```

## Parent

```jsx
<To be first parent of={ CHILD_OF } />
<To be last parent of={ CHILD_OF } />
<To be direct parent number={5} of={ CHILD_OF } />
<To be direct parent at={5} of={ CHILD_OF } />
```

### Direct parent

```jsx
<To be first direct parent of={ CHILD_OF } />
<To be last direct parent of={ CHILD_OF } />
<To be direct direct parent number={5} of={ CHILD_OF } />
<To be direct direct parent at={5} of={ CHILD_OF } />
```

## Sibling

```jsx
<To be a sibling of={ CHILD_OF } />
<To be first sibling of={ CHILD_OF } />
<To be last sibling of={ CHILD_OF } />
<To be sibling number={5} of={ CHILD_OF } />
<To be sibling at={5} of={ CHILD_OF } />
```

## Direct sibling

```jsx
<To be a direct sibling of={ CHILD_OF } />
<To be first direct sibling of={ CHILD_OF } />
<To be last direct sibling of={ CHILD_OF } />
<To be direct sibling number={5} of={ CHILD_OF } />
<To be direct sibling at={5} of={ CHILD_OF } />
```

## Previous sibling

```jsx
<To be previous sibling of={ CHILD_OF } />
<To be first previous sibling of={ CHILD_OF } />
<To be last previous sibling of={ CHILD_OF } />
<To be previous sibling number={5} of={ CHILD_OF } />
<To be previous sibling at={5} of={ CHILD_OF } />
```

## Next sibling

```jsx
<To be next sibling of={ CHILD_OF } />
<To be first next sibling of={ CHILD_OF } />
<To be last next sibling of={ CHILD_OF } />
<To be next sibling number={5} of={ CHILD_OF } />
<To be next sibling at={5} of={ CHILD_OF } />
```
